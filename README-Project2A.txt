The file P2random.h is needed for PR mode.

The spec input files (TL and PR) are included, along with correct
output for various flags (none, just v, just m, just i, just t, all).

The output should be the same no matter which input file (TL or PR)
it is run with.
